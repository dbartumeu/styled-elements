import breakpoints from './breakpoints';

export const defaults = {
  pseudoSelectors: ['hover', 'active', 'focus', 'selected'],
  breakpoints: breakpoints,
  fgColor: '#333',
  bgColor: '#fafafa',
  borderWidth: '1px',
  borderStyle: 'solid',
  borderColor: '#ddd'
};
